<h1>商品一覧</h1>
<p><%= Item.count %> 件の商品があります。</p>

<ul>
  <% Item.all.each do |item| %>
    <li>
      <%= image_tag(item.image) %>
      <h2><%= item.name %></h2>
      <p><%= item.description %></p>
      <p>価格: <%= number_to_currency(item.price) %></p>
    </li>
  <% end %>
</ul>