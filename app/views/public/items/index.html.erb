<h1>商品一覧</h1>
<p><%= Item.count %> 件の商品があります。</p>
<ul>
  <% Item.all.each do |item| %>
    <li>
      <%= link_to public_path(item) do %>
        <%= image_tag("cake.jpg", style: "width: 200px; height: 200px;") %>
      <% end %>
      <h2><%= item.name %></h2>
      <p><%= item.explanation %></p>
      <p>価格: <%= number_to_currency(item.tax_excluded_price) %></p>
    </li>
  <% end %>
</ul>